stages:
  - conflict

variables:
  GIT_TERMINAL_PROMPT: "0"

generate_and_push_conflict:
  stage: conflict
  only:
    - development
  tags:
    - runner
  resource_group: conflict-lock
  script:
    - node conflict-job.js
