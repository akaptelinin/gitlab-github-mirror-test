stages:
  - conflict

variables:
  GIT_TERMINAL_PROMPT: "0"

generate_and_push_conflict:
  stage: conflict
  only:
    - development
  tags:
    - runner
  script:
    - 'echo "PWD: $PWD"'
    - dir
    - node conflict-job.js
